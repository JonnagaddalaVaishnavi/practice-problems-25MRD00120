package mits.mca.classtopics;

/*
The binary number system only uses two digits 0 & 1,and number system can be called binary string.
You're required to implement the following functions. * int operationBinaryString(Char* str).
The function accepts a string str as its argument, the string str contains of binary digits separated within alphabets as follows -
1. -A denotes AND operation
    B denotes OR operation
    C denotes XOR operation.
You're required to calculate the result of the string str, scanning the string to the right taking one operation at a time and return the same.
Note :- No order of priorities of operations is required.
        length of string is ODD.
        if str is null or NaN return -1.
Input :- 1C0C1C1A0B1
Output :- 1
 */

public class BinaryNumberSystem {
    public static void main(String[] args) {

    }
}
